export enum ChatRoomType {
    PRIVATE = 'private',
    GROUP = 'group'
}

export enum ChatMessageType {
    TEXT = 'text',
    IMAGE = 'image',
    VIDEO = 'video'
}

export enum ChatMessageStatus {
    PENDING = 'pending',
    SENT = 'sent',
    DELETED = 'deleted'
}